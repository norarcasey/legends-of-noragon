node_modules
dist
dist-ssr
*.local
.DS_Store
coverage
*.tsbuildinfo
.vite

# Domain-model SVGs are rendered by the CI docs job (and locally via
# `npm run docs:render`), not committed.
docs/domain-model/*.svg
